# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 따라야 할 **큰 원칙**만 담는다.
구현 세부(스택·아키텍처·계약·컨벤션·하네스 운용)는 `.claude/skills/`의 프로젝트 스킬이 필요한 시점에 로드된다.

프로젝트: **WannaSong** — 누구나 폰으로 신청하고 스피커 PC 한 대가 YouTube로 재생하는 실시간 주크박스. 공개 저장소(MIT), 협업 2인.
이 저장소는 **모노레포**다: 웹과 공유 계약(`packages/contract`), 백엔드에 건네는 스펙(`docs/`)이 여기 있고, 백엔드는 별도 저장소에서 계약·스펙만 보고 구현한다. 워크스페이스는 늘어날 수 있다(백엔드 편입 등).
현재 스택은 `wannasong-stack` 스킬이 **단일 진실 소스**다. 이 파일은 스택 이름을 박아 두지 않는다 — 스택은 바뀔 수 있고, 바뀌면 하네스가 따라가야 한다(§8).

---

## Workflow Orchestration

### 1. Plan First
- 사소하지 않은 작업(3단계 이상, 또는 구조·계약·스택·워크스페이스 결정을 포함)에는 **먼저 계획을 글로 쓴다**(`tasks/todo.md`). 어긋나기 시작하면 멈추고 재계획한다 — 그냥 밀어붙이지 않는다.
- 모호함은 상세 스펙을 먼저 써서 줄인다. 검증 단계도 계획에 넣는다.

### 2. Subagent Strategy
- 위임은 **크고 독립적이며 병렬 실행이 가능한 작업에만** — 여러 파일·디렉터리를 넓게 훑는 탐색, 서로 겹치지 않는 리서치 갈래. 파일 1~2개 찾기, 심볼 위치 확인은 직접 한다.
- **자기 결과 확인용 위임은 금지.** 방금 내가 쓴 것을 다른 에이전트에게 "봐 달라"고 넘기는 건 위임이 아니라 미루기다. 다시 봐야 하면 내가 다시 읽는다.
- 동시 스폰 수는 낮게. 서브에이전트 1개당 **하나의 방향**만. 위임하면 결과를 마지막 메시지에 `경로:라인` + 스니펫으로 인라인 반환하도록 명시한다.

### 3. Self-Improvement Loop
- 사용자에게 **교정받을 때마다** `tasks/lessons.md`에 무엇이 잘못됐고 어떤 **자기 규칙(Hard Rule)**으로 막을지 기록한다. 날짜·상황·규칙 세 요소를 갖춘다.
- 세션 시작 시 lessons를 **먼저 검토**한다(SessionStart 훅이 최근 제목을 보여 준다).
- lessons는 **입력**이고 스킬이 **출력**이다: 쌓인 교훈은 `wannasong-skill-curator`가 스킬·규칙으로 승격한다(`/commit` 중 자동, `/skill-audit` 수동). 같은 실수가 반복되면 규칙이 약한 것이니 규칙을 고친다.

### 4. Verification Before Done
- 동작을 **증명하지 않고는** 완료라 하지 않는다.
- **타입체크·빌드 통과·dev 서버 기동은 검증이 아니다.** "검증"은 런타임 동작 확인(라우트에 실제 요청을 보내 응답을 보고, 소켓 이벤트·KV 값을 관찰)에만 쓴다. 절차는 `/verify-runtime`.
- 자동으로 확인할 수 없는 것(브라우저 렌더링, YouTube IFrame 실제 재생, 스피커 소리, QR 스캔, Java 서버가 없어 못 친 소켓 경로)은 **"확인 불가 — 육안 필요"**로 정직하게 말한다. 됐다고 단정하지 않는다.
- **검증 강도는 변경 크기에 맞춘다.** 대부분은 내가 직접 실행·로그·시연으로 증명하면 끝. 별도 리뷰 lane(`code-reviewer`/`verifier`)은 리스크가 크거나 스스로 판단이 갈릴 때만 — 모든 비사소 작업에 일괄 강제하지 않는다. 이 항목이 글로벌 OMC 템플릿의 일괄 강제보다 우선한다.
- 실제로 호출하지 않은 워크플로우/스킬 이름을 보고에 쓰지 않는다. 검증 주장에는 증거(명령·출력·`파일:라인`)를 붙인다.

### 5. Demand Elegance (Balanced)
- 사소하지 않은 변경에는 "**더 우아한 방법이 있는가?**"를 한 번 묻는다. hacky하면 지금 아는 것을 종합해 다시 구현한다.
- 단순·명확한 수정에는 건너뛴다. 과한 엔지니어링 금지.

### 6. Autonomous Bug Fixing
- 버그 리포트를 받으면 **그냥 고친다**. 로그·에러·실패 테스트를 가리키고 해결한다. 사용자에게 컨텍스트 스위칭을 강요하지 않는다.

### 7. Apply, Don't Suggest
- 해결 방법을 찾았으면 **그 자리에서 적용**한다. 스니펫만 던지고 떠넘기지 않는다. 단순 수정은 무조건 직접 적용.
- 영향 범위가 크거나 destructive한 변경(소켓 이벤트·KV 스키마 같은 **계약 변경**, 워크스페이스 추가/삭제, 스택 교체, 삭제)은 **risk를 적시해 한 번만 확인**한다. "수정할까요?" 같은 모호한 질문 금지.
- 변경 후 영향 받는 호출부(계약 ↔ 웹 클라이언트 ↔ 테스트 ↔ 계약 문서)까지 **연쇄 적용**한 뒤 보고한다.

### 8. Harness Follows the Stack (스택·아키텍처 변경 → 하네스 자동 동기화)
- 스택 파일(루트·워크스페이스의 `package.json`, 락파일, `pnpm-workspace.yaml`, tsconfig, `vercel.json`, `.env.example`, 새 최상위·워크스페이스 디렉터리, Java/Go 등 비노드 마커, CI 워크플로) 또는 하네스 파일이 바뀌면 PostToolUse 훅이 `harness-check`를 돌려 **드리프트를 컨텍스트로 주입**한다. Bash로 편집해도 감시 파일 서명으로 잡는다.
- 주입을 받으면 **사용자에게 미루지 않고** `wannasong-harness-sync` 절차대로 그 작업 안에서 스킬·CLAUDE.md·커맨드·검사기를 갱신하고, `--strict`가 0건일 때 `--update`로 manifest를 승인한다. 이것이 "완료"의 일부다.
- 스택 사실(런타임·프레임워크·실행/테스트 명령·배포·env)은 `wannasong-stack` 한 곳에만 쓴다. 다른 스킬·커맨드는 그 스킬을 가리키고 스택 이름을 재기술하지 않는다 — 드리프트 표면을 한 곳으로 모으기 위해서다.
- 라우트·컴포넌트·데이터 흐름 같은 **구조 사실**은 `wannasong-architecture`, 소켓 이벤트·KV 키·에러 코드 같은 **계약 사실**은 `wannasong-contract`를 같은 방식으로 갱신한다.
- 검사기 자체를 고쳤으면 `node .claude/harness/harness-check.mjs --selftest`로 검사기가 결함을 여전히 잡는지 증명한다.

---

## Task Management

1. **Plan First**: 계획을 `tasks/todo.md`에 체크 가능한 항목으로 적는다.
2. **Verify Plan**: 구조·계약·스택 결정이 포함되면 구현 전 계획을 확인받는다. 단순 수정은 바로 간다.
3. **Track Progress**: 진행하며 항목을 완료 처리한다.
4. **Explain Changes**: 각 단계에서 변경 요약을 high-level로 설명한다.
5. **Document Results**: `tasks/todo.md`에 리뷰 섹션을 덧붙인다. 완료된 계획·리포트는 `tasks/archive/`로 옮긴다.
6. **Capture Lessons**: 교정받은 후 `tasks/lessons.md`를 갱신한다.

---

## Core Principles

- **Simplicity First**: 모든 변경을 가능한 한 단순하게. 코드를 최소한으로만 건드린다.
- **No Laziness**: 근본 원인을 찾는다. 임시방편 금지. 시니어 개발자의 기준을 적용한다.
- **Minimal Impact**: 변경은 필요한 곳에만. 새 버그를 만들지 않는다.
- **No Unsolicited Tests**: 테스트는 사용자가 명시 요청("테스트 써줘", "tdd")할 때만 작성한다. 내 변경으로 기존 테스트가 깨지면 최소 수정으로 맞춘다. 검증은 런타임 동작 확인 중심.
- **Korean First**: 응답·주석·커밋 메시지·UI 문구는 한국어. 코드 식별자·로그 태그는 영어.

---

## 프로젝트 스킬 (구현 세부 사항)

### 자동 활성 스킬 (description 기반 자동 로드)

| 스킬 | 다룰 때 사용 |
|------|-------------|
| `wannasong-stack` | **기술 스택 단일 진실 소스** — 모노레포 구성, 워크스페이스별 런타임·프레임워크·실행/테스트/빌드 명령·배포 타깃·환경변수, 백엔드(실시간·API) 계획. 스택이 바뀌면 **코드보다 이 스킬을 먼저** 갱신 |
| `wannasong-architecture` | 웹 워크스페이스 구조 — 라우트(페이지·Route Handler), 컴포넌트·훅·lib 역할, Vercel/Java 역할 분리, 데이터 흐름(REST vs 소켓 vs KV), 클라이언트 상태·localStorage, API 라우트 작성 패턴 |
| `wannasong-contract` | **Java 연동 계약 단일 진실 소스** — 소켓 이벤트·페이로드·ack, KV 키·스키마, 에러 코드→문구, 레이트리밋 수치, env 동등성. 계약을 바꿀 때의 동시 갱신 규칙 |
| `wannasong-conventions` | TypeScript/React 스타일, 파일 배치, 주석·문구 언어, 커밋 메시지(`type(scope): 한국어`), 커밋 단위, 버전, 테스트 정책 |
| `wannasong-harness-ops` | **하네스 자신을 손댈 때** — 규칙을 판단형으로 쓰는 법, 사고 강도 기준, 리뷰 보고 형식, 문서 분량 상한, 스택 가변성 원칙 |
| `wannasong-harness-sync` | **스택/구조/계약 드리프트를 감지했을 때** — 하네스를 새 사실에 맞게 갱신·검증·승인하는 절차. 훅 주입 `[harness-check]`가 트리거. 새 워크스페이스(백엔드) 추가 시 절차 포함 |
| `wannasong-skill-curator` | 스킬 자기개선 루프(Inspect→Develop→Curate). lessons·자동메모리의 미반영 교훈을 스킬 패치로 승격, 검사기가 못 보는 품질(description·연결) 점검 |

### 슬래시 커맨드

| 상황 | 명령 |
|------|------|
| 변경 사항 커밋 (하네스 게이트 + 스킬 루프 포함) | `/commit` |
| 스킬 전수 점검·디벨롭·정리 | `/skill-audit` |
| 하네스 정합성 검사·동기화·검사기 자기검증 | `/harness-check` |
| 웹을 띄워 REST·소켓 런타임 동작 검증 | `/verify-runtime` |
| Route Handler(API 라우트) 신규 추가 | `/new-api-route` |

자동 실행 조건
- 세션 시작 → SessionStart 훅이 `harness-check --session` 실행(드리프트·최근 lessons·열린 todo).
- 스택/하네스 파일 변경(도구 무관) → PostToolUse 훅이 `harness-check --hook` 실행 → 결과가 있으면 §8 절차.
- 코드 변경을 마무리할 때 → `/verify-runtime`으로 런타임 확인.
- 커밋 시 → `/commit`이 `harness-check --strict`와 스킬 루프를 함께 돈다.
- 계약(`packages/contract`)을 바꾸면 → `wannasong-contract`의 동시 갱신 규칙(타입·문서·클라이언트·테스트·상대 저장소 통보).

---

## 보조 컨텍스트

- `apps/web/CLAUDE.md`는 프레임워크가 생성한 Next.js 버전 주의 문서를 가리킨다. 그 워크스페이스에서 작업할 때 함께 로드되며, 프레임워크 API는 설치된 패키지의 docs를 우선한다.
- 글로벌 `~/.claude/CLAUDE.md`의 "Opus 5 오버라이드" 절(모델 라우팅·위임·검증 강도)은 이 저장소에도 적용된다. 본 파일 §2·§4와 같은 방향이다.
- OMC가 `.claude/` 아래 자체 CLAUDE.md를 두면 그 템플릿의 일괄 강제 문구보다 본 파일 §4가 우선한다.
